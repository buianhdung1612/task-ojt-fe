export const Title = (props) => {
    const { title } = props;

    return (
        <>
            <h1 className="text-[#202020] font-[700] text-[26px] mt-[6px] mb-[20px]">{title}</h1>
        </>
    )
}